# Exercise 4: ES Module

## Problem

We have the same code from exercise 01.

## Solution

Use `export` to only expose the functions.
Use `import` in the main script to import the functions and use them.
## Reference

See the `solution/` folder for a working implementation.
